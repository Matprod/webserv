Notes pour Matheo:
-> Deux servers sur le meme port Interedit
-> Tu ne dois pas automatiquement fermer le fd client (voir ...)
-> CGI actuelle ne prennent pas en compte le bon server (utilise request.config)

I - Test Response/Config

	A - Test Global Config
		1 - Test Parser 
			ROOT:
			/
			/tmp/www
			/
			/var/www/html

			-> Error if no root in global config; OK


			METHOD_ALLOW:
			-> If no allow_methods in global config: allow_methods = GET, POST, DELETE; OK
			-> If allow_methods present but empty: allow_methods = NONE; OK



	B - Test Location Config


II - Test Method Response:

	A - GET

	B - POST

	C - DELETE


III - Code Security and Cleaness:

	A - Leak

	B - Fd / Socket not Closed

	C - Resilience / Pressure Test

	D - Edge Cases that might Segfault

IV - Check Subject Requirements:

	-> Server is Non-Blocking at all times:
	-> Check Errno after read/write Not Allowed:
	-> What are the use of the macro (FD_SET, FD_CLR...?)
	-> Handle request Time out
	-> Server is compatible with Google
	-> Compare Behaviors with NGINX (Config Parsing, Status Code...)
	-> Default Error Pages if none are provided: YES (But don't handle those provided);
	-> Does our Server, serve a Fully static Website: YES 
	-> The first server for a host:port will be the default for this host:port (meaning it
		will respond to all requests that do not belong to another server).
	-> How does Redirect work ?

